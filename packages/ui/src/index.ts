// Scaffold placeholder — replaced by this package's build tickets (spec: docs/spec/).
// The core re-export exercises workspace-link + exports-map type resolution (no project references).
export { definePlugin } from "@switchboard-dev/core";
export const PACKAGE_NAME = "@switchboard-dev/ui";
