---
"@switchboard-dev/core": minor
---

Kernel spec §2–§4: the name grammar and name kinds, `definePlugin` with manifest validation, and the activation lifecycle with Disposable teardown — plus the diagnostics machinery they lean on (`SwitchboardError`, the kernel diagnostics channel, dev mode, and the default console reporter).
